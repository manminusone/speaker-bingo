extends layout

block content
  div(class="container")
    i(class="fa fa-user fa-my-icon fa-pull-right")
    h1 Log in

    p If you've already signed up, you can log in here. Note that your account need to be activated before you can log in. Check your email for an activation message if you haven't already done so.

    p If you need to create an account, 
      a(href="/signup") you can do that here.

    - if (message)
      div(class="alert alert-danger alert-dismissible" role="alert")
        button(type="button" class="close" data-dismiss="alert" aria-label="Close")
            span(aria-hidden="true") 
              i(class="fa fa-times-circle")
        strong Error: 
        | #{message}

    form(method="post")
        table
          tr
            th Your email address &nbsp;
            td
              input(type="email" class="form-control" id="email" name="email" value=email placeholder="email@address.here")
          tr
            th Password &nbsp;
              td
                input(type="password" class="form-control" id="pwd" name="pwd" placeholder="password")
          tr
            td(colspan="2") &nbsp;
          tr
            th Ready?
            td
              input(type="submit" value="Yep, let's go")

